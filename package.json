{
  "name": "hugocornellier.com",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "nodemon backend/server.js",
    "server": "node backend/server.js",
    "build": "npm install --legacy-peer-deps && npm install --legacy-peer-deps --prefix frontend && npm run build --prefix frontend"
  },
  "keywords": [
    "mkrecords",
    "hugocornellier.com"
  ],
  "author": "Hugo Cornellier",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "geoip-lite": "^1.4.10",
    "socket.io": "^4.1.2",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
